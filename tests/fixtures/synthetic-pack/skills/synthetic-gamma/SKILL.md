---
name: synthetic-gamma
description: Synthetic test fixture skill gamma. Use only for skill-os internal logic tests; never invoke or install in a real project.
allowed-tools: Read, Write, Edit, Bash, Glob
---

# Synthetic Gamma

Third skill in the `synthetic-pack` fixture. Tests use this as an `optional`
skill to verify the inventory + scaffolder differentiate `skill-required`
from `skill-optional` classes.
